Bugs to fix
1:  Add 1 IC to list
	Add a different one to list
	List defaults back to first on entered
	should display new one
	FIXED

2: Not drawing multiple image circles...
	FIXED


3: After toggling off 'draw all film types' it draws the 35mm frame instead of your previous selected one
	FIXED

4: When drawing all image circles
   if smallest ic is toggled as the one to draw before drawing all,
   then when untoggling draw all ic then the item at the bottom of the list will be drawn
   FIXED